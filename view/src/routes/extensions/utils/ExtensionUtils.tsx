import { Box, styled, Typography } from '@mui/material';

export const ExtensionHeaderText = styled(Typography)({
    fontSize: '18px',
    marginLeft: '8px',
});

export const ExtensionHeaderBox = styled(Box)({
    display: 'flex',
    alignItems: 'center',
    margin: '8px',
});
